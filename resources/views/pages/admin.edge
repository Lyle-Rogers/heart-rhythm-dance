@layout('main')

@section('extracss')
  {{ style('pages/admin') }}
@endsection

@section('content')
  <div class="admin_content">
    <div class="column_labels_wrapper">
      <div class="extra_space_for_studentname">
        <div class="student_name_column">
          Student Name
        </div>
      </div>
      <div class="age_group_column">
        Age Group
      </div>
      <div class="payment_method_column">
        Payment Method
      </div>
      <div class="extra_space_for_paymentssofar">
        <div class="payments_so_far_column">
          Payments so Far
        </div>
      </div>
      <div class="styles_column">
        Styles
      </div>
      <div class="username_column">
        User Name
      </div>
      <div class="gender_column">
        Gender
      </div>
      <div class="age_column">
        Age
      </div>
      <div class="extra_space_for_dateofbirth">
        <div class="date_of_birth_column">
          Date of Birth
        </div>
      </div>
      <div class="emergency_contacts_column">
        Emergency Contacts
      </div>
      <div class="name_of_parents_column">
        Name of Parents
      </div>
      <div class="health_conditions_column">
        Health Conditions
      </div>
      <div class="sign_column">
        Sign
      </div>
      <div class="making_date_centered">
        <div class="date_column">
          Date
        </div>
      </div>
    </div>
    <div class="rows_wrapper">
      @each(register in registers)
        <div class="row_container">
          <div class="student_name">
            @if(register.student_name === null)
            @else
              <p>{{ register.student_name }}</p>
            @endif
          </div>
          <div class="age_group">
            @if(register.age_group === null)
            @else
              <p>{{ register.age_group }}</p>
            @endif
          </div>
          <div class="payment_method">
            @if(register.cash_payment_monthly === true)
              <p>Cash<p>
            @endif
            @if(register.online_payment_monthly === true)
              <p>Online<p>
            @endif
          </div>
          <div class="payments_so_far">
            @if(register.payments === null)
            @else
              <p>{{ register.payments }}</p>
            @endif
          </div>
          <div class="styles">
            @if(register.styles.includes('Tec & Choreography'))
              <div class="style">Tec & Choreography</div>
            @endif
            @if(register.styles.includes('Tumbling & Acro'))
              <div class="style">Tumbling & Acro</div>
            @endif
            @if(register.styles.includes('Ballet & Pointe'))
              <div class="style">Ballet & Pointe</div>
            @endif
            @if(register.styles.includes('Hip Hop'))
              <div class="style">Hip Hop</div>
            @endif
            @if(register.styles.includes('Aerial Arts'))
              <div class="style">Aerial Arts</div>
            @endif
            @if(register.styles.includes('Jazz Funk'))
              <div class="style">Jazz Funk</div>
            @endif
            @if(register.styles.includes('Yoga'))
              <div class="style">Yoga</div>
            @endif
            @if(register.styles.includes('Barre Fitness'))
              <div class="style">Barre Fitness</div>
            @endif
            @if(register.styles.includes('Belly Dance'))
              <div class="style">Belly Dance</div>
            @endif
          </div>
          <div class="username">
            @if(register.user === null)
            @else
              <p>{{ register.user }}</p>
            @endif
          </div>
          <div class="gender">
            @if(register.male === true)
              <p>Male<p>
            @endif
            @if(register.female === true)
              <p>Female<p>
            @endif
          </div>
          <div class="age">
            @if(register.age === null)
            @else
              <p>{{ register.age }}</p>
            @endif
          </div>
          <div class="date_of_birth">
            @if(register.date_of_birth === null)
            @else
              <p>{{ register.date_of_birth }}</p>
            @endif
          </div>
          <div class="emergency_contacts">
            @if(register.emergency_contacts === null)
            @else
              <p>{{ register.emergency_contacts }}</p>
            @endif
          </div>
          <div class="name_of_parents">
            @if(register.name_of_parents === null)
            @else
              <p>{{ register.name_of_parents }}</p>
            @endif
          </div>
          <div class="health_conditions">
            @if(register.health_conditions === null)
            @else
              <p>{{ register.health_conditions }}</p>
            @endif
          </div>
          <div class="sign">
            @if(register.sign === null)
            @else
              <p>{{ register.sign }}</p>
            @endif
          </div>
          <div class="date">
            @if(register.date === null)
            @else
              <p>{{ register.date }}</p>
            @endif
          </div>
        </div>
      @endeach
    </div>
  </div>
@endsection