@layout('main')

@section('extracss')
  {{ style('pages/calendar') }}
@endsection

@section('content')
  <div class="calendar_content">
    <div class="months_wrapper">

      <div class="month">
        <h1 class="month_name">JANUARY</h1>
        <div class="days">
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan1'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">1</h3>
              @loggedIn
                @if(descriptionClicked == 'jan1')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan1.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan1' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan1.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan1.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan1')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan2'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">2</h3>
              @loggedIn
                @if(descriptionClicked == 'jan2')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan2.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan2' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan2.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan2.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan2')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan3'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">3</h3>
              @loggedIn
                @if(descriptionClicked == 'jan3')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan3.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan3' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan3.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan3.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan3')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan4'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">4</h3>
              @loggedIn
                @if(descriptionClicked == 'jan4')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan4.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan4' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan4.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan4.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan4')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan5'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">5</h3>
              @loggedIn
                @if(descriptionClicked == 'jan5')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan5.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan5' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan5.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan5.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan5')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan6'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">6</h3>
              @loggedIn
                @if(descriptionClicked == 'jan6')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan6.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan6' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan6.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan6.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan6')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan7'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">7</h3>
              @loggedIn
                @if(descriptionClicked == 'jan7')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan7.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan7' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan7.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan7.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan7')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan8'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">8</h3>
              @loggedIn
                @if(descriptionClicked == 'jan8')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan8.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan8' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan8.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan8.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan8')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan9'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">9</h3>
              @loggedIn
                @if(descriptionClicked == 'jan9')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan9.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan9' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan9.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan9.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan9')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan10'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">10</h3>
              @loggedIn
                @if(descriptionClicked == 'jan10')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan10.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan10' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan10.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan10.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan10')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan11'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">11</h3>
              @loggedIn
                @if(descriptionClicked == 'jan11')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan11.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan11' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan11.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan11.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan11')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan12'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">12</h3>
              @loggedIn
                @if(descriptionClicked == 'jan12')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan12.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan12' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan12.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan12.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan12')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan13'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">13</h3>
              @loggedIn
                @if(descriptionClicked == 'jan13')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan13.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan13' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan13.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan13.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan13')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan14'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">14</h3>
              @loggedIn
                @if(descriptionClicked == 'jan14')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan14.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan14' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan14.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan14.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan14')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan15'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">15</h3>
              @loggedIn
                @if(descriptionClicked == 'jan15')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan15.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan15' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan15.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan15.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan15')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan16'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">16</h3>
              @loggedIn
                @if(descriptionClicked == 'jan16')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan16.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan16' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan16.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan16.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan16')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan17'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">17</h3>
              @loggedIn
                @if(descriptionClicked == 'jan17')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan17.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan17' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan17.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan17.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan17')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan18'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">18</h3>
              @loggedIn
                @if(descriptionClicked == 'jan18')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan18.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan18' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan18.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan18.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan18')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan19'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">19</h3>
              @loggedIn
                @if(descriptionClicked == 'jan19')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan19.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan19' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan19.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan19.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan19')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan20'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">20</h3>
              @loggedIn
                @if(descriptionClicked == 'jan20')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan20.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan20' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan20.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan20.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan20')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan21'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">21</h3>
              @loggedIn
                @if(descriptionClicked == 'jan21')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan21.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan21' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan21.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan21.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan21')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan22'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">22</h3>
              @loggedIn
                @if(descriptionClicked == 'jan22')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan22.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan22' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan22.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan22.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan22')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan23'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">23</h3>
              @loggedIn
                @if(descriptionClicked == 'jan23')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan23.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan23' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan23.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan23.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan23')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan24'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">24</h3>
              @loggedIn
                @if(descriptionClicked == 'jan24')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan24.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan24' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan24.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan24.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan24')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan25'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">25</h3>
              @loggedIn
                @if(descriptionClicked == 'jan25')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan25.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan25' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan25.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan25.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan25')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan26'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">26</h3>
              @loggedIn
                @if(descriptionClicked == 'jan26')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan26.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan26' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan26.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan26.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan26')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan27'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">27</h3>
              @loggedIn
                @if(descriptionClicked == 'jan27')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan27.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan27' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan27.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan27.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan27')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan28'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">28</h3>
              @loggedIn
                @if(descriptionClicked == 'jan28')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan28.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan28' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan28.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan28.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan28')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan29'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">29</h3>
              @loggedIn
                @if(descriptionClicked == 'jan29')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan29.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan29' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan29.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan29.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan29')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan30'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">30</h3>
              @loggedIn
                @if(descriptionClicked == 'jan30')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan30.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan30' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan30.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan30.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan30')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jan31'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">31</h3>
              @loggedIn
                @if(descriptionClicked == 'jan31')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jan31.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jan31' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jan31.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jan31.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jan31')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
        </div>
      </div>

      <div class="month">
        <h1 class="month_name">FEBRUARY</h1>
        <div class="days">
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'feb1'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">1</h3>
              @loggedIn
                @if(descriptionClicked == 'feb1')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ feb1.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'feb1' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ feb1.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ feb1.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'feb1')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'feb2'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">2</h3>
              @loggedIn
                @if(descriptionClicked == 'feb2')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ feb2.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'feb2' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ feb2.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ feb2.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'feb2')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'feb3'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">3</h3>
              @loggedIn
                @if(descriptionClicked == 'feb3')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ feb3.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'feb3' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ feb3.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ feb3.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'feb3')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'feb4'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">4</h3>
              @loggedIn
                @if(descriptionClicked == 'feb4')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ feb4.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'feb4' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ feb4.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ feb4.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'feb4')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'feb5'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">5</h3>
              @loggedIn
                @if(descriptionClicked == 'feb5')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ feb5.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'feb5' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ feb5.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ feb5.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'feb5')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'feb6'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">6</h3>
              @loggedIn
                @if(descriptionClicked == 'feb6')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ feb6.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'feb6' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ feb6.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ feb6.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'feb6')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'feb7'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">7</h3>
              @loggedIn
                @if(descriptionClicked == 'feb7')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ feb7.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'feb7' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ feb7.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ feb7.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'feb7')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'feb8'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">8</h3>
              @loggedIn
                @if(descriptionClicked == 'feb8')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ feb8.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'feb8' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ feb8.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ feb8.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'feb8')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'feb9'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">9</h3>
              @loggedIn
                @if(descriptionClicked == 'feb9')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ feb9.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'feb9' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ feb9.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ feb9.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'feb9')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'feb10'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">10</h3>
              @loggedIn
                @if(descriptionClicked == 'feb10')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ feb10.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'feb10' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ feb10.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ feb10.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'feb10')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'feb11'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">11</h3>
              @loggedIn
                @if(descriptionClicked == 'feb11')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ feb11.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'feb11' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ feb11.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ feb11.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'feb11')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'feb12'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">12</h3>
              @loggedIn
                @if(descriptionClicked == 'feb12')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ feb12.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'feb12' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ feb12.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ feb12.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'feb12')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'feb13'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">13</h3>
              @loggedIn
                @if(descriptionClicked == 'feb13')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ feb13.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'feb13' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ feb13.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ feb13.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'feb13')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'feb14'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">14</h3>
              @loggedIn
                @if(descriptionClicked == 'feb14')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ feb14.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'feb14' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ feb14.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ feb14.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'feb14')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'feb15'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">15</h3>
              @loggedIn
                @if(descriptionClicked == 'feb15')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ feb15.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'feb15' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ feb15.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ feb15.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'feb15')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'feb16'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">16</h3>
              @loggedIn
                @if(descriptionClicked == 'feb16')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ feb16.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'feb16' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ feb16.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ feb16.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'feb16')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'feb17'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">17</h3>
              @loggedIn
                @if(descriptionClicked == 'feb17')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ feb17.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'feb17' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ feb17.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ feb17.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'feb17')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'feb18'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">18</h3>
              @loggedIn
                @if(descriptionClicked == 'feb18')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ feb18.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'feb18' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ feb18.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ feb18.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'feb18')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'feb19'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">19</h3>
              @loggedIn
                @if(descriptionClicked == 'feb19')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ feb19.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'feb19' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ feb19.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ feb19.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'feb19')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'feb20'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">20</h3>
              @loggedIn
                @if(descriptionClicked == 'feb20')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ feb20.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'feb20' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ feb20.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ feb20.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'feb20')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'feb21'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">21</h3>
              @loggedIn
                @if(descriptionClicked == 'feb21')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ feb21.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'feb21' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ feb21.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ feb21.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'feb21')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'feb22'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">22</h3>
              @loggedIn
                @if(descriptionClicked == 'feb22')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ feb22.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'feb22' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ feb22.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ feb22.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'feb22')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'feb23'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">23</h3>
              @loggedIn
                @if(descriptionClicked == 'feb23')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ feb23.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'feb23' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ feb23.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ feb23.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'feb23')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'feb24'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">24</h3>
              @loggedIn
                @if(descriptionClicked == 'feb24')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ feb24.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'feb24' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ feb24.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ feb24.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'feb24')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'feb25'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">25</h3>
              @loggedIn
                @if(descriptionClicked == 'feb25')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ feb25.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'feb25' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ feb25.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ feb25.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'feb25')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'feb26'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">26</h3>
              @loggedIn
                @if(descriptionClicked == 'feb26')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ feb26.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'feb26' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ feb26.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ feb26.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'feb26')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'feb27'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">27</h3>
              @loggedIn
                @if(descriptionClicked == 'feb27')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ feb27.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'feb27' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ feb27.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ feb27.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'feb27')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'feb28'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">28</h3>
              @loggedIn
                @if(descriptionClicked == 'feb28')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ feb28.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'feb28' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ feb28.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ feb28.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'feb28')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
        </div>
      </div>

      <div class="month">
        <h1 class="month_name">MARCH</h1>
        <div class="days">
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar1'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">1</h3>
              @loggedIn
                @if(descriptionClicked == 'mar1')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar1.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar1' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar1.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar1.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar1')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar2'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">2</h3>
              @loggedIn
                @if(descriptionClicked == 'mar2')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar2.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar2' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar2.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar2.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar2')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar3'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">3</h3>
              @loggedIn
                @if(descriptionClicked == 'mar3')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar3.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar3' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar3.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar3.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar3')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar4'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">4</h3>
              @loggedIn
                @if(descriptionClicked == 'mar4')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar4.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar4' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar4.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar4.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar4')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar5'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">5</h3>
              @loggedIn
                @if(descriptionClicked == 'mar5')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar5.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar5' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar5.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar5.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar5')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar6'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">6</h3>
              @loggedIn
                @if(descriptionClicked == 'mar6')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar6.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar6' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar6.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar6.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar6')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar7'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">7</h3>
              @loggedIn
                @if(descriptionClicked == 'mar7')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar7.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar7' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar7.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar7.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar7')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar8'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">8</h3>
              @loggedIn
                @if(descriptionClicked == 'mar8')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar8.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar8' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar8.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar8.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar8')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar9'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">9</h3>
              @loggedIn
                @if(descriptionClicked == 'mar9')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar9.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar9' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar9.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar9.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar9')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar10'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">10</h3>
              @loggedIn
                @if(descriptionClicked == 'mar10')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar10.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar10' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar10.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar10.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar10')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar11'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">11</h3>
              @loggedIn
                @if(descriptionClicked == 'mar11')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar11.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar11' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar11.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar11.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar11')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar12'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">12</h3>
              @loggedIn
                @if(descriptionClicked == 'mar12')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar12.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar12' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar12.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar12.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar12')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar13'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">13</h3>
              @loggedIn
                @if(descriptionClicked == 'mar13')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar13.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar13' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar13.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar13.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar13')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar14'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">14</h3>
              @loggedIn
                @if(descriptionClicked == 'mar14')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar14.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar14' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar14.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar14.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar14')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar15'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">15</h3>
              @loggedIn
                @if(descriptionClicked == 'mar15')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar15.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar15' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar15.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar15.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar15')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar16'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">16</h3>
              @loggedIn
                @if(descriptionClicked == 'mar16')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar16.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar16' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar16.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar16.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar16')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar17'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">17</h3>
              @loggedIn
                @if(descriptionClicked == 'mar17')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar17.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar17' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar17.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar17.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar17')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar18'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">18</h3>
              @loggedIn
                @if(descriptionClicked == 'mar18')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar18.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar18' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar18.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar18.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar18')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar19'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">19</h3>
              @loggedIn
                @if(descriptionClicked == 'mar19')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar19.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar19' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar19.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar19.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar19')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar20'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">20</h3>
              @loggedIn
                @if(descriptionClicked == 'mar20')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar20.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar20' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar20.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar20.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar20')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar21'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">21</h3>
              @loggedIn
                @if(descriptionClicked == 'mar21')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar21.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar21' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar21.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar21.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar21')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar22'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">22</h3>
              @loggedIn
                @if(descriptionClicked == 'mar22')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar22.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar22' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar22.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar22.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar22')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar23'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">23</h3>
              @loggedIn
                @if(descriptionClicked == 'mar23')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar23.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar23' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar23.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar23.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar23')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar24'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">24</h3>
              @loggedIn
                @if(descriptionClicked == 'mar24')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar24.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar24' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar24.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar24.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar24')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar25'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">25</h3>
              @loggedIn
                @if(descriptionClicked == 'mar25')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar25.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar25' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar25.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar25.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar25')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar26'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">26</h3>
              @loggedIn
                @if(descriptionClicked == 'mar26')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar26.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar26' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar26.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar26.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar26')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar27'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">27</h3>
              @loggedIn
                @if(descriptionClicked == 'mar27')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar27.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar27' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar27.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar27.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar27')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar28'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">28</h3>
              @loggedIn
                @if(descriptionClicked == 'mar28')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar28.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar28' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar28.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar28.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar28')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar29'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">29</h3>
              @loggedIn
                @if(descriptionClicked == 'mar29')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar29.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar29' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar29.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar29.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar29')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar30'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">30</h3>
              @loggedIn
                @if(descriptionClicked == 'mar30')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar30.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar30' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar30.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar30.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar30')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'mar31'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">31</h3>
              @loggedIn
                @if(descriptionClicked == 'mar31')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ mar31.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'mar31' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ mar31.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ mar31.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'mar31')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
        </div>
      </div>

      <div class="month">
        <h1 class="month_name">APRIL</h1>
        <div class="days">
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr1'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">1</h3>
              @loggedIn
                @if(descriptionClicked == 'apr1')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr1.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr1' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr1.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr1.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr1')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr2'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">2</h3>
              @loggedIn
                @if(descriptionClicked == 'apr2')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr2.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr2' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr2.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr2.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr2')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr3'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">3</h3>
              @loggedIn
                @if(descriptionClicked == 'apr3')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr3.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr3' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr3.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr3.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr3')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr4'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">4</h3>
              @loggedIn
                @if(descriptionClicked == 'apr4')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr4.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr4' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr4.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr4.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr4')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr5'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">5</h3>
              @loggedIn
                @if(descriptionClicked == 'apr5')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr5.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr5' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr5.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr5.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr5')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr6'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">6</h3>
              @loggedIn
                @if(descriptionClicked == 'apr6')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr6.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr6' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr6.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr6.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr6')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr7'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">7</h3>
              @loggedIn
                @if(descriptionClicked == 'apr7')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr7.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr7' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr7.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr7.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr7')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr8'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">8</h3>
              @loggedIn
                @if(descriptionClicked == 'apr8')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr8.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr8' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr8.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr8.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr8')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr9'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">9</h3>
              @loggedIn
                @if(descriptionClicked == 'apr9')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr9.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr9' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr9.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr9.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr9')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr10'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">10</h3>
              @loggedIn
                @if(descriptionClicked == 'apr10')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr10.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr10' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr10.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr10.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr10')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr11'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">11</h3>
              @loggedIn
                @if(descriptionClicked == 'apr11')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr11.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr11' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr11.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr11.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr11')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr12'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">12</h3>
              @loggedIn
                @if(descriptionClicked == 'apr12')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr12.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr12' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr12.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr12.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr12')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr13'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">13</h3>
              @loggedIn
                @if(descriptionClicked == 'apr13')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr13.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr13' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr13.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr13.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr13')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr14'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">14</h3>
              @loggedIn
                @if(descriptionClicked == 'apr14')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr14.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr14' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr14.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr14.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr14')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr15'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">15</h3>
              @loggedIn
                @if(descriptionClicked == 'apr15')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr15.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr15' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr15.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr15.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr15')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr16'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">16</h3>
              @loggedIn
                @if(descriptionClicked == 'apr16')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr16.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr16' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr16.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr16.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr16')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr17'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">17</h3>
              @loggedIn
                @if(descriptionClicked == 'apr17')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr17.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr17' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr17.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr17.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr17')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr18'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">18</h3>
              @loggedIn
                @if(descriptionClicked == 'apr18')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr18.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr18' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr18.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr18.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr18')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr19'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">19</h3>
              @loggedIn
                @if(descriptionClicked == 'apr19')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr19.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr19' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr19.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr19.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr19')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr20'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">20</h3>
              @loggedIn
                @if(descriptionClicked == 'apr20')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr20.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr20' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr20.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr20.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr20')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr21'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">21</h3>
              @loggedIn
                @if(descriptionClicked == 'apr21')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr21.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr21' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr21.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr21.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr21')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr22'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">22</h3>
              @loggedIn
                @if(descriptionClicked == 'apr22')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr22.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr22' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr22.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr22.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr22')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr23'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">23</h3>
              @loggedIn
                @if(descriptionClicked == 'apr23')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr23.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr23' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr23.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr23.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr23')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr24'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">24</h3>
              @loggedIn
                @if(descriptionClicked == 'apr24')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr24.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr24' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr24.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr24.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr24')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr25'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">25</h3>
              @loggedIn
                @if(descriptionClicked == 'apr25')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr25.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr25' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr25.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr25.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr25')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr26'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">26</h3>
              @loggedIn
                @if(descriptionClicked == 'apr26')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr26.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr26' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr26.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr26.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr26')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr27'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">27</h3>
              @loggedIn
                @if(descriptionClicked == 'apr27')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr27.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr27' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr27.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr27.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr27')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr28'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">28</h3>
              @loggedIn
                @if(descriptionClicked == 'apr28')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr28.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr28' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr28.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr28.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr28')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr29'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">29</h3>
              @loggedIn
                @if(descriptionClicked == 'apr29')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr29.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr29' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr29.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr29.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr29')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'apr30'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">30</h3>
              @loggedIn
                @if(descriptionClicked == 'apr30')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ apr30.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'apr30' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ apr30.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ apr30.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'apr30')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
        </div>
      </div>

      <div class="month">
        <h1 class="month_name">MAY</h1>
        <div class="days">
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may1'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">1</h3>
              @loggedIn
                @if(descriptionClicked == 'may1')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may1.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may1' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may1.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may1.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may1')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may2'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">2</h3>
              @loggedIn
                @if(descriptionClicked == 'may2')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may2.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may2' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may2.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may2.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may2')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may3'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">3</h3>
              @loggedIn
                @if(descriptionClicked == 'may3')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may3.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may3' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may3.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may3.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may3')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may4'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">4</h3>
              @loggedIn
                @if(descriptionClicked == 'may4')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may4.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may4' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may4.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may4.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may4')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may5'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">5</h3>
              @loggedIn
                @if(descriptionClicked == 'may5')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may5.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may5' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may5.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may5.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may5')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may6'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">6</h3>
              @loggedIn
                @if(descriptionClicked == 'may6')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may6.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may6' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may6.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may6.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may6')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may7'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">7</h3>
              @loggedIn
                @if(descriptionClicked == 'may7')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may7.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may7' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may7.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may7.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may7')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may8'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">8</h3>
              @loggedIn
                @if(descriptionClicked == 'may8')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may8.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may8' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may8.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may8.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may8')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may9'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">9</h3>
              @loggedIn
                @if(descriptionClicked == 'may9')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may9.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may9' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may9.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may9.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may9')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may10'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">10</h3>
              @loggedIn
                @if(descriptionClicked == 'may10')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may10.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may10' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may10.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may10.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may10')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may11'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">11</h3>
              @loggedIn
                @if(descriptionClicked == 'may11')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may11.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may11' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may11.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may11.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may11')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may12'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">12</h3>
              @loggedIn
                @if(descriptionClicked == 'may12')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may12.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may12' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may12.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may12.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may12')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may13'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">13</h3>
              @loggedIn
                @if(descriptionClicked == 'may13')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may13.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may13' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may13.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may13.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may13')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may14'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">14</h3>
              @loggedIn
                @if(descriptionClicked == 'may14')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may14.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may14' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may14.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may14.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may14')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may15'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">15</h3>
              @loggedIn
                @if(descriptionClicked == 'may15')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may15.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may15' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may15.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may15.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may15')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may16'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">16</h3>
              @loggedIn
                @if(descriptionClicked == 'may16')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may16.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may16' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may16.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may16.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may16')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may17'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">17</h3>
              @loggedIn
                @if(descriptionClicked == 'may17')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may17.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may17' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may17.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may17.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may17')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may18'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">18</h3>
              @loggedIn
                @if(descriptionClicked == 'may18')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may18.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may18' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may18.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may18.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may18')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may19'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">19</h3>
              @loggedIn
                @if(descriptionClicked == 'may19')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may19.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may19' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may19.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may19.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may19')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may20'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">20</h3>
              @loggedIn
                @if(descriptionClicked == 'may20')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may20.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may20' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may20.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may20.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may20')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may21'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">21</h3>
              @loggedIn
                @if(descriptionClicked == 'may21')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may21.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may21' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may21.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may21.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may21')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may22'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">22</h3>
              @loggedIn
                @if(descriptionClicked == 'may22')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may22.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may22' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may22.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may22.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may22')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may23'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">23</h3>
              @loggedIn
                @if(descriptionClicked == 'may23')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may23.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may23' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may23.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may23.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may23')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may24'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">24</h3>
              @loggedIn
                @if(descriptionClicked == 'may24')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may24.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may24' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may24.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may24.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may24')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may25'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">25</h3>
              @loggedIn
                @if(descriptionClicked == 'may25')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may25.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may25' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may25.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may25.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may25')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may26'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">26</h3>
              @loggedIn
                @if(descriptionClicked == 'may26')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may26.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may26' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may26.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may26.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may26')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may27'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">27</h3>
              @loggedIn
                @if(descriptionClicked == 'may27')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may27.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may27' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may27.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may27.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may27')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may28'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">28</h3>
              @loggedIn
                @if(descriptionClicked == 'may28')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may28.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may28' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may28.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may28.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may28')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may29'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">29</h3>
              @loggedIn
                @if(descriptionClicked == 'may29')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may29.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may29' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may29.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may29.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may29')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may30'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">30</h3>
              @loggedIn
                @if(descriptionClicked == 'may30')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may30.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may30' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may30.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may30.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may30')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'may31'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">31</h3>
              @loggedIn
                @if(descriptionClicked == 'may31')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ may31.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'may31' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ may31.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ may31.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'may31')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
        </div>
      </div>

      <div class="month">
        <h1 class="month_name">JUNE</h1>
        <div class="days">
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun1'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">1</h3>
              @loggedIn
                @if(descriptionClicked == 'jun1')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun1.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun1' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun1.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun1.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun1')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun2'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">2</h3>
              @loggedIn
                @if(descriptionClicked == 'jun2')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun2.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun2' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun2.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun2.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun2')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun3'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">3</h3>
              @loggedIn
                @if(descriptionClicked == 'jun3')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun3.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun3' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun3.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun3.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun3')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun4'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">4</h3>
              @loggedIn
                @if(descriptionClicked == 'jun4')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun4.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun4' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun4.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun4.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun4')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun5'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">5</h3>
              @loggedIn
                @if(descriptionClicked == 'jun5')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun5.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun5' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun5.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun5.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun5')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun6'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">6</h3>
              @loggedIn
                @if(descriptionClicked == 'jun6')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun6.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun6' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun6.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun6.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun6')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun7'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">7</h3>
              @loggedIn
                @if(descriptionClicked == 'jun7')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun7.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun7' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun7.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun7.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun7')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun8'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">8</h3>
              @loggedIn
                @if(descriptionClicked == 'jun8')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun8.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun8' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun8.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun8.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun8')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun9'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">9</h3>
              @loggedIn
                @if(descriptionClicked == 'jun9')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun9.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun9' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun9.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun9.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun9')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun10'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">10</h3>
              @loggedIn
                @if(descriptionClicked == 'jun10')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun10.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun10' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun10.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun10.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun10')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun11'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">11</h3>
              @loggedIn
                @if(descriptionClicked == 'jun11')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun11.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun11' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun11.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun11.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun11')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun12'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">12</h3>
              @loggedIn
                @if(descriptionClicked == 'jun12')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun12.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun12' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun12.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun12.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun12')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun13'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">13</h3>
              @loggedIn
                @if(descriptionClicked == 'jun13')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun13.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun13' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun13.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun13.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun13')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun14'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">14</h3>
              @loggedIn
                @if(descriptionClicked == 'jun14')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun14.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun14' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun14.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun14.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun14')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun15'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">15</h3>
              @loggedIn
                @if(descriptionClicked == 'jun15')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun15.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun15' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun15.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun15.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun15')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun16'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">16</h3>
              @loggedIn
                @if(descriptionClicked == 'jun16')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun16.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun16' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun16.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun16.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun16')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun17'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">17</h3>
              @loggedIn
                @if(descriptionClicked == 'jun17')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun17.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun17' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun17.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun17.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun17')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun18'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">18</h3>
              @loggedIn
                @if(descriptionClicked == 'jun18')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun18.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun18' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun18.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun18.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun18')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun19'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">19</h3>
              @loggedIn
                @if(descriptionClicked == 'jun19')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun19.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun19' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun19.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun19.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun19')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun20'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">20</h3>
              @loggedIn
                @if(descriptionClicked == 'jun20')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun20.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun20' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun20.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun20.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun20')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun21'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">21</h3>
              @loggedIn
                @if(descriptionClicked == 'jun21')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun21.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun21' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun21.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun21.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun21')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun22'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">22</h3>
              @loggedIn
                @if(descriptionClicked == 'jun22')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun22.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun22' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun22.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun22.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun22')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun23'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">23</h3>
              @loggedIn
                @if(descriptionClicked == 'jun23')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun23.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun23' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun23.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun23.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun23')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun24'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">24</h3>
              @loggedIn
                @if(descriptionClicked == 'jun24')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun24.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun24' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun24.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun24.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun24')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun25'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">25</h3>
              @loggedIn
                @if(descriptionClicked == 'jun25')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun25.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun25' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun25.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun25.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun25')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun26'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">26</h3>
              @loggedIn
                @if(descriptionClicked == 'jun26')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun26.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun26' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun26.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun26.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun26')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun27'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">27</h3>
              @loggedIn
                @if(descriptionClicked == 'jun27')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun27.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun27' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun27.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun27.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun27')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun28'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">28</h3>
              @loggedIn
                @if(descriptionClicked == 'jun28')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun28.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun28' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun28.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun28.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun28')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun29'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">29</h3>
              @loggedIn
                @if(descriptionClicked == 'jun29')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun29.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun29' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun29.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun29.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun29')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jun30'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">30</h3>
              @loggedIn
                @if(descriptionClicked == 'jun30')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jun30.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jun30' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jun30.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jun30.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jun30')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
        </div>
      </div>

      <div class="month">
        <h1 class="month_name">JULY</h1>
        <div class="days">
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul1'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">1</h3>
              @loggedIn
                @if(descriptionClicked == 'jul1')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul1.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul1' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul1.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul1.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul1')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul2'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">2</h3>
              @loggedIn
                @if(descriptionClicked == 'jul2')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul2.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul2' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul2.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul2.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul2')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul3'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">3</h3>
              @loggedIn
                @if(descriptionClicked == 'jul3')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul3.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul3' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul3.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul3.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul3')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul4'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">4</h3>
              @loggedIn
                @if(descriptionClicked == 'jul4')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul4.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul4' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul4.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul4.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul4')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul5'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">5</h3>
              @loggedIn
                @if(descriptionClicked == 'jul5')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul5.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul5' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul5.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul5.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul5')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul6'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">6</h3>
              @loggedIn
                @if(descriptionClicked == 'jul6')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul6.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul6' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul6.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul6.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul6')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul7'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">7</h3>
              @loggedIn
                @if(descriptionClicked == 'jul7')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul7.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul7' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul7.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul7.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul7')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul8'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">8</h3>
              @loggedIn
                @if(descriptionClicked == 'jul8')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul8.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul8' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul8.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul8.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul8')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul9'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">9</h3>
              @loggedIn
                @if(descriptionClicked == 'jul9')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul9.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul9' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul9.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul9.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul9')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul10'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">10</h3>
              @loggedIn
                @if(descriptionClicked == 'jul10')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul10.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul10' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul10.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul10.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul10')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul11'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">11</h3>
              @loggedIn
                @if(descriptionClicked == 'jul11')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul11.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul11' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul11.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul11.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul11')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul12'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">12</h3>
              @loggedIn
                @if(descriptionClicked == 'jul12')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul12.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul12' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul12.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul12.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul12')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul13'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">13</h3>
              @loggedIn
                @if(descriptionClicked == 'jul13')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul13.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul13' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul13.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul13.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul13')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul14'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">14</h3>
              @loggedIn
                @if(descriptionClicked == 'jul14')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul14.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul14' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul14.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul14.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul14')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul15'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">15</h3>
              @loggedIn
                @if(descriptionClicked == 'jul15')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul15.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul15' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul15.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul15.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul15')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul16'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">16</h3>
              @loggedIn
                @if(descriptionClicked == 'jul16')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul16.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul16' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul16.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul16.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul16')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul17'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">17</h3>
              @loggedIn
                @if(descriptionClicked == 'jul17')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul17.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul17' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul17.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul17.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul17')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul18'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">18</h3>
              @loggedIn
                @if(descriptionClicked == 'jul18')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul18.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul18' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul18.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul18.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul18')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul19'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">19</h3>
              @loggedIn
                @if(descriptionClicked == 'jul19')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul19.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul19' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul19.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul19.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul19')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul20'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">20</h3>
              @loggedIn
                @if(descriptionClicked == 'jul20')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul20.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul20' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul20.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul20.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul20')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul21'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">21</h3>
              @loggedIn
                @if(descriptionClicked == 'jul21')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul21.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul21' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul21.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul21.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul21')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul22'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">22</h3>
              @loggedIn
                @if(descriptionClicked == 'jul22')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul22.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul22' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul22.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul22.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul22')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul23'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">23</h3>
              @loggedIn
                @if(descriptionClicked == 'jul23')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul23.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul23' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul23.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul23.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul23')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul24'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">24</h3>
              @loggedIn
                @if(descriptionClicked == 'jul24')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul24.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul24' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul24.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul24.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul24')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul25'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">25</h3>
              @loggedIn
                @if(descriptionClicked == 'jul25')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul25.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul25' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul25.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul25.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul25')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul26'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">26</h3>
              @loggedIn
                @if(descriptionClicked == 'jul26')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul26.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul26' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul26.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul26.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul26')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul27'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">27</h3>
              @loggedIn
                @if(descriptionClicked == 'jul27')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul27.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul27' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul27.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul27.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul27')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul28'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">28</h3>
              @loggedIn
                @if(descriptionClicked == 'jul28')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul28.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul28' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul28.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul28.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul28')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul29'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">29</h3>
              @loggedIn
                @if(descriptionClicked == 'jul29')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul29.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul29' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul29.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul29.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul29')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul30'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">30</h3>
              @loggedIn
                @if(descriptionClicked == 'jul30')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul30.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul30' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul30.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul30.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul30')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'jul31'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">31</h3>
              @loggedIn
                @if(descriptionClicked == 'jul31')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ jul31.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'jul31' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ jul31.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ jul31.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'jul31')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
        </div>
      </div>

      <div class="month">
        <h1 class="month_name">AUGUST</h1>
        <div class="days">
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug1'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">1</h3>
              @loggedIn
                @if(descriptionClicked == 'aug1')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug1.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug1' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug1.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug1.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug1')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug2'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">2</h3>
              @loggedIn
                @if(descriptionClicked == 'aug2')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug2.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug2' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug2.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug2.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug2')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug3'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">3</h3>
              @loggedIn
                @if(descriptionClicked == 'aug3')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug3.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug3' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug3.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug3.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug3')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug4'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">4</h3>
              @loggedIn
                @if(descriptionClicked == 'aug4')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug4.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug4' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug4.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug4.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug4')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug5'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">5</h3>
              @loggedIn
                @if(descriptionClicked == 'aug5')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug5.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug5' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug5.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug5.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug5')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug6'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">6</h3>
              @loggedIn
                @if(descriptionClicked == 'aug6')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug6.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug6' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug6.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug6.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug6')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug7'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">7</h3>
              @loggedIn
                @if(descriptionClicked == 'aug7')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug7.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug7' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug7.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug7.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug7')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug8'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">8</h3>
              @loggedIn
                @if(descriptionClicked == 'aug8')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug8.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug8' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug8.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug8.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug8')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug9'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">9</h3>
              @loggedIn
                @if(descriptionClicked == 'aug9')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug9.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug9' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug9.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug9.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug9')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug10'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">10</h3>
              @loggedIn
                @if(descriptionClicked == 'aug10')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug10.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug10' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug10.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug10.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug10')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug11'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">11</h3>
              @loggedIn
                @if(descriptionClicked == 'aug11')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug11.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug11' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug11.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug11.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug11')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug12'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">12</h3>
              @loggedIn
                @if(descriptionClicked == 'aug12')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug12.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug12' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug12.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug12.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug12')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug13'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">13</h3>
              @loggedIn
                @if(descriptionClicked == 'aug13')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug13.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug13' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug13.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug13.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug13')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug14'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">14</h3>
              @loggedIn
                @if(descriptionClicked == 'aug14')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug14.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug14' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug14.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug14.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug14')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug15'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">15</h3>
              @loggedIn
                @if(descriptionClicked == 'aug15')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug15.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug15' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug15.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug15.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug15')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug16'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">16</h3>
              @loggedIn
                @if(descriptionClicked == 'aug16')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug16.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug16' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug16.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug16.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug16')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug17'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">17</h3>
              @loggedIn
                @if(descriptionClicked == 'aug17')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug17.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug17' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug17.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug17.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug17')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug18'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">18</h3>
              @loggedIn
                @if(descriptionClicked == 'aug18')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug18.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug18' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug18.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug18.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug18')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug19'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">19</h3>
              @loggedIn
                @if(descriptionClicked == 'aug19')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug19.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug19' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug19.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug19.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug19')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug20'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">20</h3>
              @loggedIn
                @if(descriptionClicked == 'aug20')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug20.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug20' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug20.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug20.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug20')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug21'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">21</h3>
              @loggedIn
                @if(descriptionClicked == 'aug21')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug21.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug21' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug21.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug21.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug21')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug22'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">22</h3>
              @loggedIn
                @if(descriptionClicked == 'aug22')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug22.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug22' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug22.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug22.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug22')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug23'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">23</h3>
              @loggedIn
                @if(descriptionClicked == 'aug23')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug23.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug23' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug23.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug23.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug23')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug24'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">24</h3>
              @loggedIn
                @if(descriptionClicked == 'aug24')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug24.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug24' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug24.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug24.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug24')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug25'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">25</h3>
              @loggedIn
                @if(descriptionClicked == 'aug25')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug25.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug25' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug25.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug25.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug25')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug26'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">26</h3>
              @loggedIn
                @if(descriptionClicked == 'aug26')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug26.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug26' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug26.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug26.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug26')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug27'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">27</h3>
              @loggedIn
                @if(descriptionClicked == 'aug27')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug27.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug27' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug27.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug27.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug27')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug28'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">28</h3>
              @loggedIn
                @if(descriptionClicked == 'aug28')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug28.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug28' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug28.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug28.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug28')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug29'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">29</h3>
              @loggedIn
                @if(descriptionClicked == 'aug29')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug29.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug29' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug29.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug29.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug29')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug30'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">30</h3>
              @loggedIn
                @if(descriptionClicked == 'aug30')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug30.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug30' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug30.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug30.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug30')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'aug31'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">31</h3>
              @loggedIn
                @if(descriptionClicked == 'aug31')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ aug31.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'aug31' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ aug31.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ aug31.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'aug31')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
        </div>
      </div>

      <div class="month">
        <h1 class="month_name">SEPTEMBER</h1>
        <div class="days">
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep1'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">1</h3>
              @loggedIn
                @if(descriptionClicked == 'sep1')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep1.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep1' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep1.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep1.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep1')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep2'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">2</h3>
              @loggedIn
                @if(descriptionClicked == 'sep2')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep2.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep2' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep2.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep2.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep2')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep3'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">3</h3>
              @loggedIn
                @if(descriptionClicked == 'sep3')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep3.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep3' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep3.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep3.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep3')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep4'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">4</h3>
              @loggedIn
                @if(descriptionClicked == 'sep4')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep4.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep4' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep4.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep4.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep4')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep5'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">5</h3>
              @loggedIn
                @if(descriptionClicked == 'sep5')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep5.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep5' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep5.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep5.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep5')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep6'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">6</h3>
              @loggedIn
                @if(descriptionClicked == 'sep6')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep6.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep6' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep6.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep6.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep6')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep7'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">7</h3>
              @loggedIn
                @if(descriptionClicked == 'sep7')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep7.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep7' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep7.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep7.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep7')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep8'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">8</h3>
              @loggedIn
                @if(descriptionClicked == 'sep8')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep8.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep8' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep8.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep8.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep8')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep9'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">9</h3>
              @loggedIn
                @if(descriptionClicked == 'sep9')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep9.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep9' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep9.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep9.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep9')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep10'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">10</h3>
              @loggedIn
                @if(descriptionClicked == 'sep10')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep10.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep10' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep10.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep10.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep10')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep11'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">11</h3>
              @loggedIn
                @if(descriptionClicked == 'sep11')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep11.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep11' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep11.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep11.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep11')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep12'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">12</h3>
              @loggedIn
                @if(descriptionClicked == 'sep12')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep12.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep12' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep12.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep12.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep12')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep13'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">13</h3>
              @loggedIn
                @if(descriptionClicked == 'sep13')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep13.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep13' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep13.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep13.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep13')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep14'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">14</h3>
              @loggedIn
                @if(descriptionClicked == 'sep14')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep14.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep14' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep14.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep14.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep14')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep15'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">15</h3>
              @loggedIn
                @if(descriptionClicked == 'sep15')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep15.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep15' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep15.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep15.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep15')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep16'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">16</h3>
              @loggedIn
                @if(descriptionClicked == 'sep16')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep16.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep16' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep16.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep16.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep16')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep17'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">17</h3>
              @loggedIn
                @if(descriptionClicked == 'sep17')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep17.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep17' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep17.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep17.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep17')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep18'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">18</h3>
              @loggedIn
                @if(descriptionClicked == 'sep18')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep18.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep18' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep18.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep18.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep18')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep19'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">19</h3>
              @loggedIn
                @if(descriptionClicked == 'sep19')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep19.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep19' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep19.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep19.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep19')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep20'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">20</h3>
              @loggedIn
                @if(descriptionClicked == 'sep20')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep20.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep20' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep20.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep20.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep20')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep21'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">21</h3>
              @loggedIn
                @if(descriptionClicked == 'sep21')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep21.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep21' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep21.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep21.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep21')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep22'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">22</h3>
              @loggedIn
                @if(descriptionClicked == 'sep22')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep22.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep22' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep22.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep22.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep22')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep23'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">23</h3>
              @loggedIn
                @if(descriptionClicked == 'sep23')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep23.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep23' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep23.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep23.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep23')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep24'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">24</h3>
              @loggedIn
                @if(descriptionClicked == 'sep24')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep24.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep24' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep24.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep24.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep24')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep25'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">25</h3>
              @loggedIn
                @if(descriptionClicked == 'sep25')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep25.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep25' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep25.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep25.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep25')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep26'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">26</h3>
              @loggedIn
                @if(descriptionClicked == 'sep26')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep26.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep26' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep26.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep26.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep26')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep27'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">27</h3>
              @loggedIn
                @if(descriptionClicked == 'sep27')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep27.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep27' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep27.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep27.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep27')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep28'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">28</h3>
              @loggedIn
                @if(descriptionClicked == 'sep28')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep28.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep28' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep28.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep28.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep28')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep29'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">29</h3>
              @loggedIn
                @if(descriptionClicked == 'sep29')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep29.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep29' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep29.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep29.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep29')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'sep30'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">30</h3>
              @loggedIn
                @if(descriptionClicked == 'sep30')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ sep30.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'sep30' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ sep30.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ sep30.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'sep30')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
        </div>
      </div>

      <div class="month">
        <h1 class="month_name">OCTOBER</h1>
        <div class="days">
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct1'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">1</h3>
              @loggedIn
                @if(descriptionClicked == 'oct1')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct1.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct1' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct1.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct1.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct1')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct2'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">2</h3>
              @loggedIn
                @if(descriptionClicked == 'oct2')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct2.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct2' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct2.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct2.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct2')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct3'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">3</h3>
              @loggedIn
                @if(descriptionClicked == 'oct3')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct3.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct3' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct3.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct3.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct3')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct4'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">4</h3>
              @loggedIn
                @if(descriptionClicked == 'oct4')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct4.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct4' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct4.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct4.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct4')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct5'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">5</h3>
              @loggedIn
                @if(descriptionClicked == 'oct5')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct5.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct5' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct5.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct5.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct5')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct6'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">6</h3>
              @loggedIn
                @if(descriptionClicked == 'oct6')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct6.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct6' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct6.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct6.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct6')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct7'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">7</h3>
              @loggedIn
                @if(descriptionClicked == 'oct7')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct7.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct7' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct7.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct7.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct7')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct8'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">8</h3>
              @loggedIn
                @if(descriptionClicked == 'oct8')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct8.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct8' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct8.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct8.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct8')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct9'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">9</h3>
              @loggedIn
                @if(descriptionClicked == 'oct9')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct9.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct9' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct9.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct9.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct9')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct10'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">10</h3>
              @loggedIn
                @if(descriptionClicked == 'oct10')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct10.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct10' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct10.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct10.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct10')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct11'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">11</h3>
              @loggedIn
                @if(descriptionClicked == 'oct11')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct11.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct11' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct11.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct11.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct11')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct12'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">12</h3>
              @loggedIn
                @if(descriptionClicked == 'oct12')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct12.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct12' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct12.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct12.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct12')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct13'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">13</h3>
              @loggedIn
                @if(descriptionClicked == 'oct13')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct13.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct13' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct13.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct13.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct13')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct14'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">14</h3>
              @loggedIn
                @if(descriptionClicked == 'oct14')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct14.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct14' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct14.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct14.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct14')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct15'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">15</h3>
              @loggedIn
                @if(descriptionClicked == 'oct15')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct15.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct15' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct15.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct15.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct15')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct16'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">16</h3>
              @loggedIn
                @if(descriptionClicked == 'oct16')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct16.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct16' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct16.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct16.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct16')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct17'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">17</h3>
              @loggedIn
                @if(descriptionClicked == 'oct17')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct17.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct17' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct17.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct17.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct17')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct18'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">18</h3>
              @loggedIn
                @if(descriptionClicked == 'oct18')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct18.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct18' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct18.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct18.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct18')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct19'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">19</h3>
              @loggedIn
                @if(descriptionClicked == 'oct19')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct19.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct19' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct19.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct19.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct19')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct20'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">20</h3>
              @loggedIn
                @if(descriptionClicked == 'oct20')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct20.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct20' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct20.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct20.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct20')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct21'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">21</h3>
              @loggedIn
                @if(descriptionClicked == 'oct21')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct21.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct21' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct21.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct21.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct21')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct22'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">22</h3>
              @loggedIn
                @if(descriptionClicked == 'oct22')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct22.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct22' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct22.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct22.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct22')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct23'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">23</h3>
              @loggedIn
                @if(descriptionClicked == 'oct23')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct23.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct23' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct23.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct23.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct23')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct24'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">24</h3>
              @loggedIn
                @if(descriptionClicked == 'oct24')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct24.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct24' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct24.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct24.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct24')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct25'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">25</h3>
              @loggedIn
                @if(descriptionClicked == 'oct25')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct25.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct25' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct25.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct25.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct25')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct26'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">26</h3>
              @loggedIn
                @if(descriptionClicked == 'oct26')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct26.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct26' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct26.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct26.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct26')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct27'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">27</h3>
              @loggedIn
                @if(descriptionClicked == 'oct27')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct27.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct27' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct27.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct27.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct27')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct28'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">28</h3>
              @loggedIn
                @if(descriptionClicked == 'oct28')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct28.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct28' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct28.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct28.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct28')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct29'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">29</h3>
              @loggedIn
                @if(descriptionClicked == 'oct29')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct29.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct29' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct29.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct29.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct29')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct30'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">30</h3>
              @loggedIn
                @if(descriptionClicked == 'oct30')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct30.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct30' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct30.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct30.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct30')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'oct31'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">31</h3>
              @loggedIn
                @if(descriptionClicked == 'oct31')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ oct31.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'oct31' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ oct31.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ oct31.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'oct31')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
        </div>
      </div>

      <div class="month">
        <h1 class="month_name">NOVEMBER</h1>
        <div class="days">
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov1'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">1</h3>
              @loggedIn
                @if(descriptionClicked == 'nov1')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov1.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov1' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov1.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov1.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov1')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov2'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">2</h3>
              @loggedIn
                @if(descriptionClicked == 'nov2')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov2.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov2' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov2.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov2.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov2')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov3'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">3</h3>
              @loggedIn
                @if(descriptionClicked == 'nov3')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov3.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov3' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov3.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov3.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov3')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov4'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">4</h3>
              @loggedIn
                @if(descriptionClicked == 'nov4')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov4.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov4' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov4.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov4.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov4')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov5'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">5</h3>
              @loggedIn
                @if(descriptionClicked == 'nov5')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov5.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov5' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov5.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov5.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov5')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov6'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">6</h3>
              @loggedIn
                @if(descriptionClicked == 'nov6')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov6.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov6' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov6.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov6.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov6')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov7'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">7</h3>
              @loggedIn
                @if(descriptionClicked == 'nov7')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov7.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov7' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov7.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov7.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov7')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov8'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">8</h3>
              @loggedIn
                @if(descriptionClicked == 'nov8')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov8.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov8' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov8.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov8.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov8')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov9'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">9</h3>
              @loggedIn
                @if(descriptionClicked == 'nov9')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov9.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov9' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov9.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov9.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov9')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov10'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">10</h3>
              @loggedIn
                @if(descriptionClicked == 'nov10')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov10.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov10' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov10.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov10.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov10')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov11'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">11</h3>
              @loggedIn
                @if(descriptionClicked == 'nov11')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov11.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov11' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov11.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov11.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov11')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov12'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">12</h3>
              @loggedIn
                @if(descriptionClicked == 'nov12')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov12.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov12' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov12.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov12.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov12')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov13'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">13</h3>
              @loggedIn
                @if(descriptionClicked == 'nov13')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov13.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov13' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov13.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov13.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov13')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov14'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">14</h3>
              @loggedIn
                @if(descriptionClicked == 'nov14')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov14.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov14' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov14.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov14.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov14')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov15'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">15</h3>
              @loggedIn
                @if(descriptionClicked == 'nov15')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov15.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov15' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov15.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov15.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov15')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov16'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">16</h3>
              @loggedIn
                @if(descriptionClicked == 'nov16')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov16.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov16' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov16.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov16.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov16')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov17'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">17</h3>
              @loggedIn
                @if(descriptionClicked == 'nov17')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov17.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov17' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov17.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov17.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov17')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov18'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">18</h3>
              @loggedIn
                @if(descriptionClicked == 'nov18')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov18.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov18' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov18.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov18.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov18')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov19'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">19</h3>
              @loggedIn
                @if(descriptionClicked == 'nov19')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov19.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov19' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov19.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov19.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov19')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov20'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">20</h3>
              @loggedIn
                @if(descriptionClicked == 'nov20')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov20.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov20' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov20.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov20.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov20')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov21'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">21</h3>
              @loggedIn
                @if(descriptionClicked == 'nov21')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov21.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov21' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov21.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov21.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov21')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov22'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">22</h3>
              @loggedIn
                @if(descriptionClicked == 'nov22')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov22.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov22' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov22.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov22.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov22')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov23'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">23</h3>
              @loggedIn
                @if(descriptionClicked == 'nov23')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov23.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov23' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov23.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov23.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov23')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov24'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">24</h3>
              @loggedIn
                @if(descriptionClicked == 'nov24')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov24.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov24' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov24.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov24.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov24')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov25'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">25</h3>
              @loggedIn
                @if(descriptionClicked == 'nov25')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov25.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov25' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov25.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov25.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov25')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov26'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">26</h3>
              @loggedIn
                @if(descriptionClicked == 'nov26')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov26.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov26' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov26.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov26.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov26')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov27'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">27</h3>
              @loggedIn
                @if(descriptionClicked == 'nov27')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov27.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov27' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov27.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov27.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov27')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov28'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">28</h3>
              @loggedIn
                @if(descriptionClicked == 'nov28')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov28.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov28' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov28.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov28.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov28')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov29'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">29</h3>
              @loggedIn
                @if(descriptionClicked == 'nov29')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov29.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov29' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov29.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov29.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov29')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'nov30'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">30</h3>
              @loggedIn
                @if(descriptionClicked == 'nov30')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ nov30.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'nov30' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ nov30.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ nov30.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'nov30')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
        </div>
      </div>

      <div class="month">
        <h1 class="month_name">DECEMBER</h1>
        <div class="days">
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec1'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">1</h3>
              @loggedIn
                @if(descriptionClicked == 'dec1')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec1.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec1' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec1.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec1.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec1')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec2'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">2</h3>
              @loggedIn
                @if(descriptionClicked == 'dec2')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec2.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec2' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec2.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec2.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec2')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec3'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">3</h3>
              @loggedIn
                @if(descriptionClicked == 'dec3')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec3.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec3' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec3.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec3.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec3')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec4'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">4</h3>
              @loggedIn
                @if(descriptionClicked == 'dec4')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec4.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec4' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec4.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec4.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec4')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec5'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">5</h3>
              @loggedIn
                @if(descriptionClicked == 'dec5')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec5.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec5' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec5.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec5.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec5')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec6'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">6</h3>
              @loggedIn
                @if(descriptionClicked == 'dec6')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec6.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec6' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec6.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec6.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec6')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec7'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">7</h3>
              @loggedIn
                @if(descriptionClicked == 'dec7')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec7.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec7' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec7.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec7.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec7')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec8'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">8</h3>
              @loggedIn
                @if(descriptionClicked == 'dec8')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec8.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec8' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec8.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec8.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec8')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec9'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">9</h3>
              @loggedIn
                @if(descriptionClicked == 'dec9')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec9.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec9' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec9.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec9.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec9')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec10'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">10</h3>
              @loggedIn
                @if(descriptionClicked == 'dec10')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec10.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec10' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec10.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec10.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec10')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec11'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">11</h3>
              @loggedIn
                @if(descriptionClicked == 'dec11')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec11.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec11' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec11.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec11.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec11')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec12'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">12</h3>
              @loggedIn
                @if(descriptionClicked == 'dec12')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec12.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec12' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec12.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec12.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec12')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec13'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">13</h3>
              @loggedIn
                @if(descriptionClicked == 'dec13')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec13.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec13' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec13.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec13.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec13')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec14'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">14</h3>
              @loggedIn
                @if(descriptionClicked == 'dec14')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec14.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec14' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec14.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec14.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec14')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec15'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">15</h3>
              @loggedIn
                @if(descriptionClicked == 'dec15')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec15.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec15' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec15.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec15.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec15')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec16'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">16</h3>
              @loggedIn
                @if(descriptionClicked == 'dec16')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec16.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec16' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec16.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec16.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec16')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec17'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">17</h3>
              @loggedIn
                @if(descriptionClicked == 'dec17')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec17.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec17' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec17.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec17.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec17')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec18'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">18</h3>
              @loggedIn
                @if(descriptionClicked == 'dec18')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec18.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec18' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec18.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec18.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec18')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec19'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">19</h3>
              @loggedIn
                @if(descriptionClicked == 'dec19')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec19.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec19' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec19.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec19.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec19')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec20'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">20</h3>
              @loggedIn
                @if(descriptionClicked == 'dec20')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec20.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec20' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec20.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec20.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec20')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec21'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">21</h3>
              @loggedIn
                @if(descriptionClicked == 'dec21')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec21.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec21' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec21.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec21.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec21')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec22'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">22</h3>
              @loggedIn
                @if(descriptionClicked == 'dec22')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec22.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec22' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec22.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec22.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec22')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec23'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">23</h3>
              @loggedIn
                @if(descriptionClicked == 'dec23')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec23.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec23' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec23.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec23.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec23')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec24'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">24</h3>
              @loggedIn
                @if(descriptionClicked == 'dec24')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec24.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec24' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec24.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec24.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec24')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec25'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">25</h3>
              @loggedIn
                @if(descriptionClicked == 'dec25')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec25.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec25' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec25.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec25.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec25')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec26'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">26</h3>
              @loggedIn
                @if(descriptionClicked == 'dec26')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec26.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec26' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec26.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec26.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec26')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec27'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">27</h3>
              @loggedIn
                @if(descriptionClicked == 'dec27')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec27.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec27' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec27.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec27.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec27')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec28'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">28</h3>
              @loggedIn
                @if(descriptionClicked == 'dec28')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec28.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec28' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec28.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec28.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec28')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec29'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">29</h3>
              @loggedIn
                @if(descriptionClicked == 'dec29')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec29.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec29' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec29.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec29.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec29')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec30'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">30</h3>
              @loggedIn
                @if(descriptionClicked == 'dec30')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec30.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec30' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec30.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec30.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec30')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
          <form action="{{ route('CalendarController.ChangeDescription', { day: 'dec31'}) }}" method="post">
            {{ csrfField() }}
            <div class="day">
              <h3 class="day_number">31</h3>
              @loggedIn
                @if(descriptionClicked == 'dec31')
                  <textarea 
                    class="description_textarea" 
                    name="description" 
                    maxLength="100"
                  >{{ dec31.description }}</textarea>
                @else
                  <a class="description_button" href="{{ route('CalendarController.descriptionClick', { day: 'dec31' }) }}">
                    <textarea 
                      class="description_textarea" 
                      readonly
                    >{{ dec31.description }}</textarea>
                  </a>
                @endif
              @else
                <textarea 
                  class="description_textarea" 
                  readonly
                >{{ dec31.description }}</textarea>
              @endloggedIn
            </div>
            @loggedIn
              @if(descriptionClicked == 'dec31')
                <button class="done_btn" type="submit">DONE</button>
              @endif
            @endloggedIn
          </form>
        </div>
      </div>

    </div>
  </div>
@endsection